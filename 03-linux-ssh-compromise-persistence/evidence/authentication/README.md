# Authentication Evidence

## Included Screenshot

### `01-auth-log-review.png`

Shows hands-on review of Linux `/var/log/auth.log`, including PAM sessions, `sudo` session activity, and CRON session entries.

**What it demonstrates:**
- familiarity with Linux authentication logs;
- recognition of `sudo` and PAM session events;
- ability to correlate authentication-related activity during Linux investigations.

**Evidence scope:** This screenshot was recovered from a related Linux security exercise. It is **not** claimed to show `10.14.94.82` or `jack-brown` from the exact SSH-compromise incident.
